#!/usr/bin/env bash
#
# A wizard — walks a human through a manual procedure step by step.
# Generated by the /wizard skill.
#
# Everything above the "STAGES" marker is the wizard library: do not hand-edit
# it. Author the per-step stages below the marker.

set -euo pipefail

# ──────────────────────────────────────────────────────────────────────────
# Wizard library — delightful, consistent UX. Identical across every wizard.
# ──────────────────────────────────────────────────────────────────────────

if [[ -t 1 ]] && command -v tput >/dev/null 2>&1 && [[ "$(tput colors 2>/dev/null || echo 0)" -ge 8 ]]; then
  BOLD=$(tput bold); DIM=$(tput dim); RESET=$(tput sgr0)
  BLUE=$(tput setaf 4); GREEN=$(tput setaf 2); YELLOW=$(tput setaf 3); RED=$(tput setaf 1)
else
  BOLD=""; DIM=""; RESET=""; BLUE=""; GREEN=""; YELLOW=""; RED=""
fi

# Author sets this at the top of the stages section.
TOTAL_STAGES=0

_STAGE_INDEX=0
ENV_FILE="${ENV_FILE:-.env}"
WRITTEN_ENV=()    # KEYs written to ENV_FILE this run
WRITTEN_SECRET=() # secret NAMEs set this run
SKIPPED=()        # things we couldn't do (e.g. gh missing)

# _clear — wipe the terminal so only the current step is on screen. No-op when
# output isn't a terminal, so piped logs stay readable.
_clear() {
  [[ -t 1 ]] || return 0
  if command -v tput >/dev/null 2>&1; then tput clear; else printf '\033[2J\033[3J\033[H'; fi
}

# banner "Title" — opening frame: what this wizard does.
banner() {
  _clear
  printf '\n%s%s  %s%s\n' "$BOLD" "$BLUE" "$1" "$RESET"
  printf '%s  %s stages%s\n\n' "$DIM" "$TOTAL_STAGES" "$RESET"
  printf '%s  You drive the browser; this wizard tells you exactly what to do and\n' "$DIM"
  printf '  captures the values you copy back. Stop any time with Ctrl-C and re-run\n'
  printf '  later — it remembers values already saved.%s\n' "$RESET"
  pause "Ready to start?"
}

# stage "Name" — clear the screen, then announce a stage and show progress.
# Clearing keeps only the current step on screen.
stage() {
  _clear
  _STAGE_INDEX=$((_STAGE_INDEX + 1))
  printf '\n%s%s▸ Stage %s/%s · %s%s\n' \
    "$BOLD" "$BLUE" "$_STAGE_INDEX" "$TOTAL_STAGES" "$1" "$RESET"
}

# say "..." — a plain instruction line.
say()  { printf '  %s\n' "$1"; }
# step "..." — a numbered-feeling action the human takes in the browser.
step() { printf '  %s•%s %s\n' "$BLUE" "$RESET" "$1"; }
note() { printf '  %s%s%s\n' "$DIM" "$1" "$RESET"; }
warn() { printf '  %s⚠ %s%s\n' "$YELLOW" "$1" "$RESET"; }

# open_url URL — open in the human's browser, cross-platform incl. WSL.
open_url() {
  local url="$1"
  printf '  %s↗ opening%s %s\n' "$GREEN" "$RESET" "$url"
  { if   command -v wslview     >/dev/null 2>&1; then wslview "$url"
    elif command -v explorer.exe >/dev/null 2>&1; then explorer.exe "$url"
    elif command -v xdg-open    >/dev/null 2>&1; then xdg-open "$url"
    elif command -v open        >/dev/null 2>&1; then open "$url"
    else warn "couldn't open a browser — visit it manually: $url"; fi
  } >/dev/null 2>&1 || warn "couldn't open a browser — visit it manually: $url"
}

# pause "msg" — wait for the human to confirm they've done the manual part.
pause() {
  printf '  %s%s%s ' "$DIM" "${1:-Press Enter to continue}" "$RESET"
  read -r _ || true
}

# confirm "question" — y/N gate; returns success on yes.
confirm() {
  local reply=""
  printf '  %s? %s [y/N] ' "$YELLOW" "$1"
  read -r reply || true
  [[ "$reply" =~ ^[Yy] ]]
}

# _existing KEY — current value of KEY in ENV_FILE, if any.
_existing() {
  [[ -f "$ENV_FILE" ]] || return 1
  local line; line=$(grep -E "^${1}=" "$ENV_FILE" | tail -n1) || return 1
  printf '%s' "${line#*=}"
}

# ask KEY "Prompt" — read a value into $KEY. Offers the existing .env value as
# a default on re-runs (Enter keeps it). Visible input (non-secret).
ask() {
  local key="$1" prompt="$2" current input
  current=$(_existing "$key" || true)
  if [[ -n "$current" ]]; then
    printf '  %s%s%s %s[Enter keeps current]%s ' "$BOLD" "$prompt" "$RESET" "$DIM" "$RESET"
  else
    printf '  %s%s%s ' "$BOLD" "$prompt" "$RESET"
  fi
  read -r input || true
  [[ -z "$input" && -n "$current" ]] && input="$current"
  printf -v "$key" '%s' "$input"
}

# ask_secret KEY "Prompt" — like ask, but input is hidden.
ask_secret() {
  local key="$1" prompt="$2" current input
  current=$(_existing "$key" || true)
  if [[ -n "$current" ]]; then
    printf '  %s%s%s %s[Enter keeps current]%s ' "$BOLD" "$prompt" "$RESET" "$DIM" "$RESET"
  else
    printf '  %s%s%s ' "$BOLD" "$prompt" "$RESET"
  fi
  read -rs input || true
  printf '\n'
  [[ -z "$input" && -n "$current" ]] && input="$current"
  printf -v "$key" '%s' "$input"
}

# write_env KEY VALUE — upsert KEY=VALUE into ENV_FILE (creates it; replaces
# any existing line). Idempotent.
write_env() {
  local key="$1" value="$2" tmp
  touch "$ENV_FILE"
  tmp=$(mktemp)
  grep -vE "^${key}=" "$ENV_FILE" > "$tmp" || true
  printf '%s=%s\n' "$key" "$value" >> "$tmp"
  mv "$tmp" "$ENV_FILE"
  WRITTEN_ENV+=("$key")
  printf '  %s✓ wrote%s %s → %s\n' "$GREEN" "$RESET" "$key" "$ENV_FILE"
}

# set_secret NAME VALUE — set a GitHub Actions repo secret via gh. Falls back
# to a warning (and records it) if gh is unavailable or unauthenticated.
set_secret() {
  local name="$1" value="$2"
  if command -v gh >/dev/null 2>&1 && gh auth status >/dev/null 2>&1; then
    if printf '%s' "$value" | gh secret set "$name" >/dev/null 2>&1; then
      WRITTEN_SECRET+=("$name")
      printf '  %s✓ set%s GitHub secret %s\n' "$GREEN" "$RESET" "$name"
      return
    fi
  fi
  SKIPPED+=("GitHub secret $name (set it manually: gh secret set $name)")
  warn "skipped GitHub secret $name — gh not ready; set it later"
}

# set_var NAME VALUE — set a GitHub Actions repo variable (non-secret).
set_var() {
  local name="$1" value="$2"
  if command -v gh >/dev/null 2>&1 && gh auth status >/dev/null 2>&1; then
    if gh variable set "$name" --body "$value" >/dev/null 2>&1; then
      printf '  %s✓ set%s GitHub variable %s\n' "$GREEN" "$RESET" "$name"
      return
    fi
  fi
  SKIPPED+=("GitHub variable $name")
  warn "skipped GitHub variable $name — gh not ready; set it later"
}

# finish — clear, then a closing summary of everything configured.
finish() {
  _clear
  printf '\n%s%s  ✓ Setup complete%s\n' "$BOLD" "$GREEN" "$RESET"
  (( ${#WRITTEN_ENV[@]} ))    && note "wrote ${#WRITTEN_ENV[@]} value(s) to $ENV_FILE: ${WRITTEN_ENV[*]}"
  (( ${#WRITTEN_SECRET[@]} )) && note "set ${#WRITTEN_SECRET[@]} GitHub secret(s): ${WRITTEN_SECRET[*]}"
  if (( ${#SKIPPED[@]} )); then
    printf '\n'; warn "still to do by hand:"
    for s in "${SKIPPED[@]}"; do note "  - $s"; done
  fi
  printf '\n'
}


# ──────────────────────────────────────────────────────────────────────────
# STAGES — instalar y autenticar un gestor de contrasenas.
# ──────────────────────────────────────────────────────────────────────────

TOTAL_STAGES=4

SECRETS_DIR="$HOME/.secrets"
MANAGER=""
HOOK=""

# --- helpers propios de este asistente ------------------------------------

# pm — que gestor de paquetes hay en esta maquina.
pm() {
  if   command -v pacman >/dev/null 2>&1; then echo pacman
  elif command -v apt-get >/dev/null 2>&1; then echo apt
  else echo desconocido; fi
}

# aur_helper — paru o yay, si hay alguno.
aur_helper() {
  if   command -v paru >/dev/null 2>&1; then echo paru
  elif command -v yay  >/dev/null 2>&1; then echo yay
  else echo ""; fi
}

# write_hook FICHERO CONTENIDO — deja un fichero en ~/.secrets con permisos 600.
# ~/.shell_common.sh carga todos los *.sh de ahi en bash y en zsh.
write_hook() {
  local name="$1" body="$2" path
  mkdir -p "$SECRETS_DIR"; chmod 700 "$SECRETS_DIR"
  path="$SECRETS_DIR/$name"
  printf '%s\n' "$body" > "$path"
  chmod 600 "$path"
  HOOK="$path"
  printf '  %s✓ escrito%s %s (modo 600)\n' "$GREEN" "$RESET" "$path"
}

# have CMD — ¿esta el binario disponible?
have() { command -v "$1" >/dev/null 2>&1; }

# ── Stage 1 ───────────────────────────────────────────────────────────────
banner "secrets-setup · gestor de contrasenas"

stage "Elegir gestor"
say "Cada opcion se autentica de forma distinta. Resumen:"
printf '\n'
step "1password  Oficial. En una maquina sin escritorio usa un service account"
note "             (token de solo lectura). Es el unico con auth desatendida real."
step "bitwarden  CLI oficial (bw). Login con email + contrasena maestra; la"
note "             sesion caduca al bloquear y hay que re-desbloquear."
step "rbw        Cliente alternativo de Bitwarden en Rust. Levanta un agente,"
note "             asi que no hay que ir pasando BW_SESSION a mano."
step "pass       Ficheros GPG en un repo git. Sin nube. Necesita una clave GPG."
printf '\n'
note "Gestor de paquetes detectado: $(pm)"
printf '\n'
_try=0
while :; do
  ask MANAGER "¿Cual? [1password / bitwarden / rbw / pass]:"
  case "$MANAGER" in
    1password|bitwarden|rbw|pass) break ;;
  esac
  _try=$((_try + 1))
  # `ask` hace `read || true`: sin stdin interactivo esto daria vueltas para
  # siempre, asi que se corta a los 3 intentos.
  if (( _try >= 3 )); then
    warn "sin respuesta valida; abortando"
    exit 1
  fi
  warn "escribe una de las cuatro"
done

# ── Stage 2 ───────────────────────────────────────────────────────────────
stage "Instalar el CLI"

case "$MANAGER" in
  1password)
    if have op; then
      note "op ya instalado: $(op --version)"
    else
      case "$(pm)" in
        pacman)
          helper=$(aur_helper)
          if [[ -n "$helper" ]]; then
            say "1password-cli vive en el AUR."
            confirm "¿Ejecuto '$helper -S --needed 1password-cli'?" && "$helper" -S --needed 1password-cli
          else
            warn "no hay paru ni yay; instala uno y vuelve a ejecutar este script"
            SKIPPED+=("instalar 1password-cli desde el AUR")
          fi
          ;;
        apt)
          say "1Password publica su propio repo apt. Hacen falta permisos de root."
          if confirm "¿Lo anado y instalo op?"; then
            curl -sS https://downloads.1password.com/linux/keys/1password.asc \
              | sudo gpg --dearmor --yes --output /usr/share/keyrings/1password-archive-keyring.gpg
            echo "deb [arch=amd64 signed-by=/usr/share/keyrings/1password-archive-keyring.gpg] https://downloads.1password.com/linux/debian/amd64 stable main" \
              | sudo tee /etc/apt/sources.list.d/1password.list >/dev/null
            sudo apt-get update -qq && sudo apt-get install -y 1password-cli
          else
            SKIPPED+=("instalar 1password-cli")
          fi
          ;;
        *) warn "gestor de paquetes desconocido; instala 'op' a mano desde 1password.com/downloads/command-line"
           SKIPPED+=("instalar 1password-cli") ;;
      esac
    fi
    ;;

  bitwarden)
    if have bw; then
      note "bw ya instalado: $(bw --version 2>/dev/null)"
    else
      say "El CLI oficial se distribuye como binario unico. Lo pongo en ~/.local/bin."
      if confirm "¿Descargo la ultima version de bw?"; then
        mkdir -p "$HOME/.local/bin"
        tmp=$(mktemp -d)
        if curl -sSL -o "$tmp/bw.zip" "https://vault.bitwarden.com/download/?app=cli&platform=linux" \
           && unzip -qo "$tmp/bw.zip" -d "$tmp"; then
          install -m 755 "$tmp/bw" "$HOME/.local/bin/bw"
          note "instalado en ~/.local/bin/bw"
        else
          warn "la descarga fallo; bajalo a mano de github.com/bitwarden/clients/releases"
          SKIPPED+=("instalar bw")
        fi
        rm -rf "$tmp"
      else
        SKIPPED+=("instalar bw")
      fi
    fi
    ;;

  rbw)
    if have rbw; then
      note "rbw ya instalado: $(rbw --version 2>/dev/null)"
    else
      case "$(pm)" in
        pacman)
          confirm "¿Ejecuto 'sudo pacman -S --needed rbw'?" && sudo pacman -S --needed rbw || SKIPPED+=("instalar rbw")
          ;;
        *)
          if have cargo; then
            say "En Debian/Ubuntu no hay paquete: se compila con cargo (tarda unos minutos)."
            confirm "¿Ejecuto 'cargo install rbw'?" && cargo install rbw || SKIPPED+=("instalar rbw")
          else
            warn "hace falta cargo (rustup) para compilar rbw"
            SKIPPED+=("instalar rbw")
          fi
          ;;
      esac
    fi
    ;;

  pass)
    if have pass; then
      note "pass ya instalado: $(pass version 2>/dev/null | head -1)"
    else
      case "$(pm)" in
        pacman) confirm "¿Ejecuto 'sudo pacman -S --needed pass'?" && sudo pacman -S --needed pass || SKIPPED+=("instalar pass") ;;
        apt)    confirm "¿Ejecuto 'sudo apt-get install -y pass'?"  && sudo apt-get install -y pass  || SKIPPED+=("instalar pass") ;;
        *)      warn "instala 'pass' con el gestor de tu distro"; SKIPPED+=("instalar pass") ;;
      esac
    fi
    ;;
esac

# ── Stage 3 ───────────────────────────────────────────────────────────────
stage "Autenticar"

case "$MANAGER" in
  1password)
    say "Un service account es un token de solo lectura para un vault concreto."
    say "Es lo que permite que un VPS lea secretos sin nadie delante."
    open_url "https://my.1password.eu/developer-tools/infrastructure-secrets/serviceaccount/"
    step "Create Service Account → ponle nombre (p.ej. 'dotfiles')"
    step "Dale acceso de solo lectura al vault que vayas a usar"
    step "Copia el token que empieza por 'ops_' — solo se muestra una vez"
    note "Si tu cuenta es .com y no .eu, cambia el dominio en la URL."
    ask_secret OP_TOKEN "Pega el token (no se vera al escribir):"
    if [[ -n "${OP_TOKEN:-}" ]]; then
      write_hook "10-1password.sh" "# generado por secrets-setup
export OP_SERVICE_ACCOUNT_TOKEN=\"$OP_TOKEN\""
    else
      warn "sin token: te quedas con la integracion de la app de escritorio, si la tienes"
      SKIPPED+=("token de service account de 1Password")
    fi
    ;;

  bitwarden)
    say "bw login pide email, contrasena maestra y, si lo tienes, el 2FA."
    say "Devuelve una clave de sesion; sin ella los comandos no leen nada."
    note "Si usas un servidor propio: bw config server https://tu-servidor"
    pause "Enter para lanzar 'bw login' (interactivo)…"
    if bw login; then
      say "Ahora desbloqueamos la boveda para obtener la sesion."
      if BW_KEY=$(bw unlock --raw); then
        write_hook "10-bitwarden.sh" "# generado por secrets-setup
# OJO: esta sesion caduca al bloquear la boveda o al reiniciar el servidor.
# Para renovarla:  bw unlock --raw   y pega el valor aqui, o re-ejecuta secrets-setup
export BW_SESSION=\"$BW_KEY\""
      else
        warn "no se pudo desbloquear"
        SKIPPED+=("obtener BW_SESSION (bw unlock --raw)")
      fi
    else
      warn "el login fallo"
      SKIPPED+=("bw login")
    fi
    ;;

  rbw)
    say "rbw guarda la configuracion y levanta un agente: no hay que pasar sesiones."
    ask RBW_EMAIL "Tu email de Bitwarden:"
    [[ -n "${RBW_EMAIL:-}" ]] && rbw config set email "$RBW_EMAIL"
    if confirm "¿Usas un servidor propio en vez de bitwarden.com?"; then
      ask RBW_URL "URL base del servidor:"
      [[ -n "${RBW_URL:-}" ]] && rbw config set base_url "$RBW_URL"
    fi
    pause "Enter para lanzar 'rbw login' (pedira la contrasena maestra)…"
    rbw login || { warn "el login fallo"; SKIPPED+=("rbw login"); }
    note "rbw no necesita ningun fichero en ~/.secrets: el agente guarda la sesion."
    ;;

  pass)
    say "pass cifra cada secreto con GPG, asi que primero hace falta una clave."
    if gpg --list-secret-keys --keyid-format=long 2>/dev/null | grep -q sec; then
      note "ya tienes al menos una clave GPG:"
      gpg --list-secret-keys --keyid-format=long 2>/dev/null | grep -A1 '^sec' | head -4
    else
      say "No hay ninguna clave secreta. Vamos a crear una."
      pause "Enter para lanzar 'gpg --full-generate-key' (elige RSA 4096, sin caducidad)…"
      gpg --full-generate-key || { warn "no se creo la clave"; SKIPPED+=("crear clave GPG"); }
    fi
    ask GPG_ID "ID o email de la clave con la que cifrar el almacen:"
    if [[ -n "${GPG_ID:-}" ]]; then
      pass init "$GPG_ID" && note "almacen creado en ~/.password-store"
      confirm "¿Lo convierto en repo git (recomendado para sincronizar)?" && pass git init
    else
      SKIPPED+=("pass init")
    fi
    note "pass no necesita nada en ~/.secrets: lee del almacen local."
    ;;
esac

# ── Stage 4 ───────────────────────────────────────────────────────────────
stage "Verificar"

say "Comprobamos que la sesion lee de verdad."
printf '\n'
ok=0
case "$MANAGER" in
  1password)
    # shellcheck source=/dev/null
    [[ -n "$HOOK" ]] && . "$HOOK"
    if have op && op whoami >/dev/null 2>&1; then
      op whoami | sed 's/^/  /'
      printf '\n  %svaults visibles:%s\n' "$BOLD" "$RESET"
      op vault list 2>/dev/null | sed 's/^/  /'
      ok=1
    fi
    ;;
  bitwarden)
    # shellcheck source=/dev/null
    [[ -n "$HOOK" ]] && . "$HOOK"
    if have bw && bw status >/dev/null 2>&1; then
      bw status | sed 's/^/  /'
      [[ "$(bw status | tr -d ' "' )" == *status:unlocked* ]] && ok=1
    fi
    ;;
  rbw)
    if have rbw && rbw unlocked >/dev/null 2>&1; then
      printf '  %s entradas en la boveda\n' "$(rbw list 2>/dev/null | wc -l)"
      ok=1
    fi
    ;;
  pass)
    if have pass && [[ -d "$HOME/.password-store" ]]; then
      pass ls 2>/dev/null | head -10 | sed 's/^/  /'
      ok=1
    fi
    ;;
esac

printf '\n'
if (( ok )); then
  printf '  %s✓ %s responde correctamente%s\n' "$GREEN" "$MANAGER" "$RESET"
  if [[ -n "$HOOK" ]]; then
    note "La credencial se carga sola en cada shell: ~/.shell_common.sh hace"
    note "source de todos los *.sh de ~/.secrets (bash y zsh)."
    note "Abre una terminal nueva o ejecuta: . $HOOK"
  fi
else
  warn "$MANAGER no respondio. Revisa los pasos anteriores y vuelve a ejecutar."
  SKIPPED+=("verificar el acceso a $MANAGER")
fi

printf '\n'
note "Este asistente NO guarda ningun secreto en el repo de dotfiles."
note "Solo escribe en ~/.secrets/, que esta fuera de chezmoi y de git."

finish
