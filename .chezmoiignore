# Rutas del source que `chezmoi apply` NO debe aplicar en $HOME.
# OJO: este fichero ES una plantilla — usa .wsl/.op del config.
# En máquinas que aún no han corrido `chezmoi init` todos evalúan a false.
#
# Solo se listan rutas que EXISTEN en el source: chezmoi jamás toca ficheros
# que no gestiona, así que listar caches/historiales/navegadores aquí no
# protege nada.

# ===== Solo viven en el repo (no son config de $HOME) =====
README.md
LICENSE
packages
bootstrap.sh
bootstrap-wsl.ps1

{{ if not .wsl -}}
# ===== Solo WSL: puentes con Windows =====
.local/bin/win-temp-path
.aliases/70-wsl.sh
{{- end }}

{{ if not .k8s -}}
# ===== Sin Kubernetes =====
.aliases/30-kubectl.sh
{{- end }}

{{ if not .ai -}}
# ===== Sin agentes de IA =====
.aliases/60-ai.sh
{{- end }}
